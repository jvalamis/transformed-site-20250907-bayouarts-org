name: transformed_site_20250907_bayouarts_org
description: A modern Flutter app generated from website data
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  eddie_renderer:
    path: ../packages/eddie_renderer

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
